scikit-learn==1.6.1
# ⚠️ Version figée volontairement, PAS ">=1.3" : c'est la version exacte utilisée
# pour entraîner les 54 modèles .pkl. Une incompatibilité de désérialisation
# entre versions scikit-learn (ex: build Docker qui installe la dernière version
# disponible) fait planter silencieusement le moteur zonal (infer_zonal.py) — le
# serveur retombe alors sur l'ancien système de secours (risk_model.py, moins
# précis) sans erreur visible côté app. Vu en prod sur le Raspberry Pi : les
# bulletins affichaient "methode_risque": "ml_gradient_boosting" au lieu de
# "ml_zonal_infer", et /api/history échouait (500) pour la plupart des zones.
joblib>=1.3
pandas>=2.0
numpy>=1.24
# Données historiques réelles (mode --openmeteo, V4.2)
openmeteo-requests>=0.3
requests-cache>=1.1
retry-requests>=2.0
